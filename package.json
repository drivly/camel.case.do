{
  "name": "camel.case.do",
  "version": "1.0.0",
  "description": "Convert JSON Object Keys to camelCase",
  "repository": "git@github.com:drivly/camel.case.do.git",
  "license": "MIT",
  "module": "worker.js",
  "scripts": {
    "dev": "wrangler dev worker.js",
    "deploy": "wrangler publish worker.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "dependencies": {
    "camelcase-keys": "^8.0.1"
  },
  "devDependencies": {
    "jest": "^28.1.3",
    "prettier": "^2.7.1",
    "wrangler": "^2.0.27"
  }
}
